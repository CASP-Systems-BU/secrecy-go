package cli

import (
	"fmt"

	"github.com/urfave/cli"
)

// run_execute is a function that executes the MPC computation on the specified computing parties cluster.
func (c *Cli) run_execute(ctx *cli.Context) error {
	fmt.Println("Executing the MPC computation on the specified computing parties cluster...")

	return nil
}
