package cli

import (
	"fmt"

	"github.com/urfave/cli"
)

func (c *Cli) run_local(ctx *cli.Context) error {
	fmt.Println("Function that builds and executes a Multi-Party Computation over the current machine ...")

	return nil
}
